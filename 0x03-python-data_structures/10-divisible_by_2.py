#!/usr/bin/python3
def divisible_by_2(my_list=[]):
    app_list = []
    for number in app_list:
        if (number % 2) == 0:
            app_list.append(True)
        else:
            app_list.append(False)
    return (app_list)
